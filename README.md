# English Diary Bot 📚

Telegram бот для изучения английского языка. Позволяет добавлять слова и проходить тесты.

## Возможности

- 📚 **Тест по всем словам** — проходите тесты по всем добавленным словам в случайном порядке
- 📝 **Тест по 30 последним словам** — тестируйте только недавно добавленные слова
- ➕ **Добавление слов** — два типа:
  - Переводы (English ↔ Русский)
  - Неправильные глаголы (3 формы: Infinitive, Past Simple, Past Participle)

## Как работает тест

1. Бот случайным образом выбирает, какое слово показать (источник или перевод)
2. Генерирует варианты ответов из вашей базы слов
3. Варианты ответов берутся только из слов того же типа:
   - Для переводов — только переводы
   - Для неправильных глаголов — только неправильные глаголы

## Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/MartellOnell/eng-diary.git
cd eng-diary
```

2. Создайте виртуальное окружение и активируйте его:
```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

5. Получите токен бота у [@BotFather](https://t.me/BotFather) и добавьте его в `.env`:
```
BOT_TOKEN=your_bot_token_here
```

6. Запустите бота:
```bash
python bot.py
```

## Структура проекта

```
eng-diary/
├── bot.py           # Основная логика бота
├── database.py      # Работа с базой данных SQLite
├── config.py        # Конфигурация
├── requirements.txt # Зависимости
├── .env.example     # Пример файла конфигурации
└── README.md        # Документация
```

## Команды бота

- `/start` — Запуск бота и регистрация
- `/cancel` — Отмена текущего действия

## Технологии

- Python 3.10+
- python-telegram-bot 21.3
- SQLite3

## Лицензия

MIT License
